## Как закоммитить и сделать PR

```bash
git status
git add .
git commit -m "your message"
git push -u origin HEAD
```

Дальше на GitHub появится кнопка **Compare & pull request** — создаёшь PR в `main`.

